import React, { Fragment } from 'react'

function Login({setAuth}) {
    return (
        <>
            <h1>Login</h1>
            <button oncl>Login</button>
        </>
    )
}

export default Login